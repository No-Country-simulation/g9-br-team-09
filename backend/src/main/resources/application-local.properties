# Datasource local
spring.datasource.url=jdbc:h2:mem:energiai;MODE=Oracle;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.clean-disabled=true
spring.flyway.validate-on-migrate=true

# Console H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=false

# JSON Web Token
# jwt.secret deve ser fornecido via variavel de ambiente JWT_SECRET
jwt.issuer=energiai-api
jwt.audience=energiai-frontend
jwt.access-token-expiration=15m

# Cookies locais trafegam por HTTP durante o desenvolvimento.
auth.refresh.cookie-secure=false
