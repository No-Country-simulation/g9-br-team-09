# Selecione uma imagem linux/amd64 identificada por tag imutavel.
# Use somente uma tag imutável com o SHA completo do commit publicado no Docker Hub.
# Exemplo: docker.io/pxs00/energiai-backend:sha-<FULL_COMMIT_SHA>
# A automação da Issue #109 altera somente esta variável no arquivo real.
BACKEND_IMAGE=docker.io/pxs00/energiai-backend:sha-<FULL_COMMIT_SHA>

# Deixe vazio na OCI para usar /opt/energiai/config/backend.env.
# Informe um caminho absoluto apenas em validacoes locais controladas.
BACKEND_ENV_FILE=

# Public HTTPS hostname served by Caddy. Replace this safe placeholder only in
# the external OCI environment file; never commit the real instance address.
API_PUBLIC_HOSTNAME=api.example.invalid

SPRING_PROFILES_ACTIVE=oci

# Limite inicial conservador para uma VM com 1 GB; confirme o comportamento na OCI.
JAVA_TOOL_OPTIONS=-XX:MaxRAMPercentage=55.0 -XX:InitialRAMPercentage=20.0 -XX:+ExitOnOutOfMemoryError

# JDBC Thin TLS sem wallet. Formato esperado: jdbc:oracle:thin:@tcps://<host>:<porta>/<service-name>
# Nao inclua aqui URL, host, service name, usuario ou senha reais.
DB_URL=
DB_USERNAME=
DB_PASSWORD=
DB_CONNECT_RETRIES=5
DB_POOL_MIN_IDLE=1
DB_POOL_MAX_SIZE=5
DB_CONNECTION_TIMEOUT_MS=30000
DB_VALIDATION_TIMEOUT_MS=5000
DB_KEEPALIVE_TIME_MS=120000

# O loopback do proprio container, em uma porta sem servico, valida intencionalmente o fallback.
# Substitua somente quando houver uma FastAPI autorizada e acessivel pela rede do container.
ML_API_BASE_URL=http://127.0.0.1:9
ML_API_CONNECT_TIMEOUT=2s
ML_API_READ_TIMEOUT=5s

# Frontend público atual. Separe múltiplas origens exatas por vírgula; nunca use *.
CORS_ALLOWED_ORIGINS=https://energiai.vercel.app

# JWT e segurança
# Gere com: openssl rand -base64 32
JWT_SECRET=
JWT_ISSUER=energiai-api
JWT_AUDIENCE=energiai-frontend
JWT_ACCESS_TOKEN_EXPIRATION=15m

# Refresh token opaco em cookie HttpOnly. Durações expressas em segundos.
AUTH_REFRESH_TOKEN_EXPIRATION=604800
AUTH_REFRESH_FAMILY_EXPIRATION=2592000
AUTH_REFRESH_REUSE_GRACE_PERIOD=5
AUTH_REFRESH_COOKIE_NAME=refresh_token
AUTH_REFRESH_COOKIE_SECURE=true
# Frontend e API em sites distintos exigem SameSite=None com Secure=true.
AUTH_REFRESH_COOKIE_SAME_SITE=None
AUTH_REFRESH_COOKIE_PATH=/api/v1/auth
# Deixe vazio para usar cookie host-only; informe somente quando houver necessidade operacional validada.
AUTH_REFRESH_COOKIE_DOMAIN=
